[pytest]
testpaths = tests
pythonpath = .
filterwarnings =
    # Raised inside starlette.testclient, not this project: starlette 1.0.0
    # still uses an alias that newer anyio releases deprecate.
    ignore:The anyio.abc.BlockingPortal alias is deprecated:DeprecationWarning
