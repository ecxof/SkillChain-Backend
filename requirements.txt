# Direct dependencies only. Transitive packages are resolved by pip.
# Versions carried over from the previous environment are pinned exactly;
# newly added packages use minimum floors until they are verified against
# a working interpreter, then pinned.

# --- Web framework ---
fastapi==0.136.1
uvicorn==0.47.0

# --- Database ---
SQLAlchemy==2.0.49
psycopg2-binary==2.9.12
alembic>=1.13

# --- Validation and configuration ---
pydantic==2.13.4
python-dotenv==1.2.2

# --- Authentication ---
python-jose[cryptography]==3.5.0

# --- HTTP client (GitHub API, OAuth token exchange) ---
httpx==0.28.1

# --- LLM provider (xAI, via the OpenAI-compatible SDK) ---
openai>=1.60

# --- Attestation timestamping (Bitcoin anchoring via OpenTimestamps) ---
opentimestamps-client>=0.7

# --- Tests ---
pytest==9.0.3
respx>=0.22
