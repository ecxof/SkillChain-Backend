# Direct dependencies only. Transitive packages are resolved by pip.
# Every version below has been checked against PyPI: it exists, is not
# yanked, and ships a wheel for Python 3.14 on Windows x64, so installing
# requires no compiler.

# --- Web framework ---
fastapi==0.136.1
uvicorn==0.47.0

# --- Database ---
SQLAlchemy==2.0.52
psycopg2-binary==2.9.13
alembic==1.20.0

# --- Validation and configuration ---
pydantic==2.13.5
python-dotenv==1.2.3

# --- Authentication ---
PyJWT==2.14.0

# --- HTTP client (GitHub API, OAuth token exchange) ---
httpx==0.28.1

# --- LLM provider (xAI, via the OpenAI-compatible SDK) ---
openai==3.13.0

# --- Attestation timestamping (Bitcoin anchoring via OpenTimestamps) ---
opentimestamps-client==0.7.2

# --- Tests ---
pytest==9.1.1
respx==0.23.1
